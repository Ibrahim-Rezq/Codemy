const Container = (props: React.HTMLAttributes<HTMLElement>) => {
    return <div className="container h-full m-auto flex flex-col justify-center items-center px-6" {...props} />
}

export default Container
