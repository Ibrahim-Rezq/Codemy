const Container = (props: React.HTMLAttributes<HTMLElement>) => {
    return <div className="container h-full m-auto flex flex-col justify-start items-center" {...props} />
}

export default Container
